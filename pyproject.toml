[project]
name = "accessflow"
version = "0.1.0"
description = "Discord onboarding bot with a configurable application flow"
license = "MIT"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiosqlite>=0.22.1",
    "loguru>=0.7.3",
    "py-cord[speed]>=2.8.1",
    "pydantic>=2.13.4",
]
